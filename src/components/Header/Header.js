import { Typography } from '@mui/material';

const Header = () => {
  return (
    <Typography color="error" variant="h2" component="h1" align="center" mb={3}>
      todos
    </Typography>
  );
};

export default Header;
